# Unraid Templates for code-server-test3

XML templates for Unraid Community Applications.

## Install
Copy `my-code-server-test3.xml` to `/boot/config/plugins/dockerMan/templates-user/` on Unraid,
then: Docker tab → Add Container → select from template list.
